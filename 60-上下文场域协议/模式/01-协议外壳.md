```sql
{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "协议外壳模式",
  "description": "用于验证字段协议外壳的模式",
  "type": "object",
  "required": ["意图", "输入", "处理", "输出", "元数据"],
  "properties": {
    "意图": {
      "type": "字符串",
      "description": "对协议目的的清晰表述"
    },
    "输入": {
      "type": "对象",
      "description": "协议所需的输入参数",
      "additionalProperties": {
        "anyOf": [
          {
            "type": "字符串",
            "description": "输入值的类型描述或占位符"
          },
          {
            "type": "对象",
            "description": "具有类型和约束的结构化输入参数"
          }
        ]
      }
    },
    "处理": {
      "type": "数组",
      "description": "要执行的操作序列",
      "items": {
        "type": "字符串",
        "description": "Pareto-lang格式的操作",
        "pattern": "^/[a-zA-Z0-9_]+\\.[a-zA-Z0-9_]+\\{.*\\}$"
      },
      "minItems": 1
    },
    "输出": {
      "type": "对象",
      "description": "协议产生的产出值",
      "additionalProperties": {
        "anyOf": [
          {
            "type": "字符串",
            "description": "产出值的类型描述或占位符"
          },
          {"type": "object",
            "description": "具有类型和格式的结构化输出参数"
          }
        ]
      }
    },
    "meta": {
      "type": "object",
      "description": "关于协议的元数据",
      "required": ["version"],
      "properties": {
        "version": {
          "type": "string",
          "description": "协议的语义版本",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        },
        "timestamp": {
          "type": "string",
          "description": "协议创建或更新的时间戳"
        },
        "author": {
          "type": "string",
          "description": "协议的作者"
        },
        "description": {
          "type": "string",
          "description": "协议的扩展描述"
        },
        "tags": {
          "type": "array",
          "description": "用于对协议进行分类的标签",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": false,
  "definitions": {
    "operationPattern": {
      "type": "string",
      "pattern": "^/[a-zA-Z0-9_]+\\.[a-zA-Z0-9_]+\\{.*\\}$",
      "description": "Pareto语言操作的模式"
    },
    "parameterPattern": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9_]+=('|\")[^'\"]*('|\")$",
      "description": "操作参数的模式"
    }
  }
}
```
